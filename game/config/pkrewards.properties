PKRewardEnabled=true
# Karma removed per point earned (unified pool)
KarmaRemovedPerPoint=10
# On death with karma: lose ALL distributed points (recoverable via karma removal)
DeathWithKarmaRemoveAllPoints=true
